# Backend URL — required for full voice demo on localhost
VITE_API_BASE_URL=http://localhost:5000

# Set to true for static Vercel demo (no backend). Auto-enabled in .env.production
# VITE_DEMO_MODE=true
